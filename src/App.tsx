import gsap from "gsap"
import { ScrollTrigger } from 'gsap/all';

gsap.registerPlugin(ScrollTrigger);

const App = ()=> {
  return (
    <div>
      <h1>Hello Vite</h1>
    </div>
  )
}

export default App
